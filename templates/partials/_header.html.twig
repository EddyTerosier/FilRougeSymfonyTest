<header>
	<nav class="navbar navbar-expand-md navbar-light bg-custom">
		<div class="container">
			<Link to="/"><a id="0" onclick="toggleActiveClass()" href="{{ path('app_home') }}" class="navbar-brand"><img src="{{ asset('assets/img/Logo transparent.png')}}" alt="Logo" width="115px"/></a></Link>
			<button class="navbar-toggler d-lg-none" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavId" aria-controls="collapsibleNavId"
                aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
			<div class="collapse navbar-collapse" id="collapsibleNavId">
				<ul class="navbar-nav align-items-start">
					<li class="nav-item"><a id = "1" class="nav-link active" href="{{ path('app_home') }}" aria-current="page" onclick="toggleActiveClass()">Accueil</a></li>
					<li class="nav-item"><a id = "2" class="nav-link" href="{{ path('app_programmes') }}" onclick="toggleActiveClass()">Programmes</a></li>
					<li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="dropdownId" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" >Giga+</a>
                        <div class="dropdown-menu" aria-labelledby="dropdownId">
					{% if app.user %}
					        <a href="{{ path("app_account", {id: app.user.id}) }}" class="dropdown-item">Mon compte <br><small>({{ app.user.firstname }})</small></a>
							<a href="{{ path("app_logout") }}" class="dropdown-item">Déconnexion</a>
					{% else %}
                            <a class="dropdown-item" id="4" href="{{path('app_registration')}}">Inscription</a>
                            <a class="dropdown-item" id="5" href="{{path('app_login')}}">Connexion</a>
					{% endif %}
                        </div>
                    </li>
				</ul>
			</div>
		</div>
	</nav>
</header>